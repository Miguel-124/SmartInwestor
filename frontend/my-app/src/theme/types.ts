export interface ThemeType {
  colors: {
    background: string;
    cardBackground: string;
    primary: string;
    primaryDark: string;
    text: string;
    placeholder: string;
    border: string;
  };
  spacing: {
    sm: number;
    md: number;
    lg: number;
  };
}
